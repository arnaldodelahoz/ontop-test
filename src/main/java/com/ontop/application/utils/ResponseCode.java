package com.ontop.application.utils;

public enum ResponseCode {
    INVALID_BODY,
    INVALID_USER,
    GENERIC_ERROR,
    INSUFFICIENT_BALANCE,
    SUCCESS,
    PAYMENT_NOT_FOUND,
    ACCOUNT_NOT_CONFIGURED,
}
